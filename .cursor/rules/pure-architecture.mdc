---
alwaysApply: true
---
Это отличный вопрос. Как консультант, я часто вижу, как команды "тонут" в файловой структуре, потому что пытаются смешать стандартный Python-backend (FastAPI/Django) и MLOps-пайплайны.

Для **Агентской системы**, построенной на принципах Чистой Архитектуры и "Flow-логики" (LangGraph/LangChain), структура должна четко разделять:
1.  **Статику** (Код, API, Базы данных).
2.  **Динамику** (Промпты, Конфигурации агентов).
3.  **Эксперименты** (Ноутбуки, Эвалюации).

Ниже представлена эталонная структура проекта (Production-Ready), которую я рекомендую для серьезных AI-стартапов и энтерпрайз-решений.

---

### Структура проекта: `ai-agent-system`

```text
.
├── .env.example             # Шаблон переменных окружения (БЕЗ КЛЮЧЕЙ!)
├── Makefile                 # Команды: make run, make test, make lint
├── pyproject.toml           # Зависимости (Poetry/UV) и конфиги линтеров (Ruff)
├── docker-compose.yml       # Локальное окружение (Postgres, Redis, Qdrant)
├── Dockerfile               # Сборка образа для продакшена
├── README.md                # Документация (Onboarding guide)
│
├── notebooks/               # 🧪 Песочница. Здесь DS проверяют гипотезы.
│   └── experimentation.ipynb  # Код отсюда НЕ ИМПОРТИРУЕТСЯ в src напрямую.
│
├── tests/                   # 🛡 Пирамида тестирования
│   ├── unit/                # Тесты домена (без I/O)
│   ├── integration/         # Тесты репозиториев и LLM адаптеров (с VCR.py)
│   └── e2e/                 # Тесты API и полных сценариев агента
│
└── src/                     # 🧠 Исходный код системы
    ├── main.py              # Точка входа (Entrypoint)
    │
    ├── config/              # ⚙️ Конфигурация приложения
    │   ├── container.py     # DI Container (Dishka / Punq / Dependency Injector)
    │   └── settings.py      # Pydantic Settings (валидация .env)
    │
    ├── domain/              # 🏰 СВЯЩЕННАЯ ЗЕМЛЯ (Чистый Python)
    │   ├── entities/        # Dataclasses: AgentState, UserProfile
    │   ├── interfaces/      # Protocols: ILLMProvider, IVectorStore, ITool
    │   ├── exceptions.py    # Ошибки: BudgetExceeded, SafetyViolation
    │   └── policies.py      # Чистые функции бизнес-правил (Guardrails логика)
    │
    ├── application/         # 🎬 ДИРИЖЕР (Бизнес-сценарии)
    │   ├── agents/          # Логика Агентов (Flow)
    │   │   ├── accountant/  # Конкретный агент "Бухгалтер"
    │   │   │   ├── graph.py # Граф LangGraph (Nodes & Edges)
    │   │   │   └── state.py # Специфичный стейт этого агента
    │   │   └── supervisor/  # Агент-менеджер (Multi-agent orchestration)
    │   ├── dtos/            # Pydantic модели для входных/выходных данных
    │   └── use_cases/       # Классические сценарии (CreateUser, GetReport)
    │
    ├── infrastructure/      # 🔌 АДАПТЕРЫ (Грязный мир)
    │   ├── llm/             # Реализация ILLMProvider
    │   │   ├── openai.py    # Адаптер к GPT-4
    │   │   └── anthropic.py # Адаптер к Claude 3.5
    │   ├── db/              # Реализация репозиториев (SQLAlchemy)
    │   │   ├── models/      # ORM модели
    │   │   └── repositories/# Реализация интерфейсов из domain
    │   ├── tools/           # Реализация ITool (Инструменты агента)
    │   │   ├── search.py    # Tavily / Google Search API
    │   │   └── calculator.py
    │   └── prompts/         # Управление промптами (Prompt Registry Adapter)
    │       ├── file_loader.py # Чтение .yaml файлов
    │       └── langfuse.py    # Или загрузка из Langfuse/Smith
    │
    ├── presentation/        # 🗣 ТОЧКА ВХОДА
    │   ├── api/             # FastAPI
    │   │   ├── v1/
    │   │   │   └── routers/ # agents.py, tasks.py
    │   │   └── dependencies.py # Инъекция зависимостей в роуты
    │   ├── telegram_bot/    # Aiogram хендлеры
    │   └── cli/             # Typer/Click команды
    │
    └── resources/           # 📝 Текстовые ресурсы (если не во внешней системе)
        └── prompts/         # YAML/Jinja2 шаблоны промптов
            ├── accountant.yaml
            └── safety.yaml
```

---

### Разбор ключевых папок для Агента

#### 1. `src/application/agents/` — Сердце Flow
Здесь живут ваши графы LangGraph.
*   **Почему здесь?** Это Use Case. "Агент Бухгалтер" — это сценарий выполнения задачи.
*   **Правило:** Один агент — одна папка. Внутри `graph.py` описывает узлы и ребра. Он импортирует интерфейсы из `domain` (например, `ILLMProvider`), но не знает, что это OpenAI.

#### 2. `src/domain/entities/` vs `state.py`
*   В `domain/entities` лежат общие сущности (User, Transaction).
*   В `application/agents/xxx/state.py` лежит `AgentState` (TypedDict для графа).
*   *Нюанс:* Если `AgentState` используется в нескольких агентах, вынесите его в `domain`. Если он уникален для графа — оставьте в `application`.

#### 3. `src/config/container.py` — Клей системы (DI)
Это **критически важный файл**. В мире Чистой Архитектуры вы не делаете `from infra import OpenAI` внутри сервиса.
Вы настраиваете DI-контейнер, который говорит:
*"Когда Агент просит `ILLMProvider`, дай ему `OpenAIAdapter` с ключом из конфига, и оберни это в `LogginMiddleware`."*

Используйте библиотеки: **Dishka** (современная, быстрая) или **Punq**. Избегайте "магии" wiring, предпочитайте явную регистрацию.

#### 4. `src/resources/prompts/` — Промпты как Ресурс
Самая частая ошибка — писать промпты внутри Python-кода строками.
*   **Как надо:** Промпты лежат в `.yaml` или `.jinja2` файлах.
*   **Почему:** Это позволяет менять поведение агента ("Ты теперь добрее") без пересборки Docker-контейнера и без изменения Python-кода.
*   **Infrastructure:** В `infra/prompts` лежит код, который умеет читать эти файлы и подставлять переменные.

#### 5. `notebooks/` — Зона отчуждения
Data Scientists любят "грязный" код. Это нормально. Но этот код не должен попадать в `src`.
*   **Правило:** Если что-то из ноутбука заработало, это нужно переписать ("продикшенизировать") в `src/domain` или `src/application` и покрыть тестами. Не импортируйте `src` в ноутбуки "криво" (через `sys.path.append`), лучше установите проект в editable режиме (`pip install -e .`).

---

### Пример потока разработки (Workflow) в этой структуре

1.  **Задача:** Добавить агенту инструмент "Поиск в Википедии".
2.  **Domain:** Идем в `src/domain/interfaces.py`, добавляем `protocol IWikiTool`.
3.  **Infrastructure:** Идем в `src/infrastructure/tools/`, создаем `wiki.py`, реализуем класс `WikipediaTool` (используя библиотеку `wikipedia`).
4.  **Application:** Идем в `src/application/agents/my_agent/graph.py`. Добавляем узел, который использует `IWikiTool` (инжектится через конструктор или стейт).
5.  **DI:** Идем в `src/config/container.py`, регистрируем `IWikiTool` -> `WikipediaTool`.
6.  **Presentation:** API не меняется. Пользователь просто спрашивает "Кто такой Наполеон?", а агент теперь умеет это гуглить.

Эта структура выдержит рост от 2 до 50 разработчиков и от 1 до 100 различных агентов.